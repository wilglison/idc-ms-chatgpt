name: CI-CD
on: 
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Docker build teste
        run: echo "Docker build teste"
      - name: lista arquivos
        run: ls -l 
 
